name: Pull Request CI

on:
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  build-and-test:
    uses: ./.github/workflows/common.yml
    secrets: inherit
