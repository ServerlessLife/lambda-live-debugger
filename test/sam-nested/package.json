{
  "name": "sam-nested",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "sam build",
    "deploy": "npm run build && sam deploy --config-env test --no-confirm-changeset --capabilities CAPABILITY_IAM CAPABILITY_AUTO_EXPAND && npm run export_outputs",
    "export_outputs": "aws cloudformation describe-stacks --stack-name lld-test-sam-nested --output json --query Stacks[0].Outputs > sam-outputs.json",
    "destroy": "sam delete --config-env test --no-prompts"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.5",
    "@types/aws-lambda": "^8.10.159"
  },
  "dependencies": {
    "@aws-sdk/client-sts": "^3.936.0"
  }
}
